numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0

# Optional — only needed for the LSTM forecasting path.
# CPU-only install is enough for prototyping:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# Optional — only needed for convert_fits.py (FITS parsing)
astropy>=5.3
